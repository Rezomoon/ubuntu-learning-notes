<!-- مقدمه و مفاهیم و برداشت من از لینوکس -->


<!-- توزیع های مهم لینوکس -->


 **Linux Disributions** :
 منظور توزیع رو میشه ی جورایی با کارایی و هدف ساختشون و ورژن بندی رسوند.

 در این جا یک لیست کوچیک از معروف ترین توزیع های لینوکس و خلاصه ای از کاربردشون رو اووردم براتون :
 | کاربردشون          |                توزیع ها                       |
|--------------------------|------------------------------------------|
| Debian **Ubuntu**            |     دسکتاپ و سرور مبتدیان            |
| Debian **Mint**              |     دسکتاپ آسان برای ویندوزکارها     | 
| Debian **Elementary**        |        دسکتاپ زیبا شبیه macOS        |
| RedHat **RHDEL**             |       سرورهای سازمانی و تجاری        |
| RedHat **Cent**              |       سرور تولید (نسخه قدیمی)        |
| RedHat **Rocky** Linux       |   سرور پایدار رایگان سازمانی         |
| RedHat **Fedora**            |        توسعه و فناوری‌های جدید        |



یک سایت برای چک کردن تمامی توزیع ها و آمار های آنها:
    
    www.Distrowatch.com

ما تمرکز اصلی مون و شروع کارمون با 
Linux Ubunto 
هست .
چرا که دارای رابط گرافیکی نزدیک به ویندوز هست و برای تازه کار ها مناسب است.D


**معماری سیستم عامل گنو/لینوکس**

(یک توزیع مبتنی بر لینوکس از ۵ بخش تشکیل میشود)

۱. kernel       : هسته سیستم عامل
##
۲. glibc        : ابزار های سیستمی
        - ی جورایی واسط بین کاربر و هسته
##
۳. system tools : ابزار های سیستمی
##
۴. Tool Chain   : ابزار های توسعه
##
۵. Applications : نرم افزار ها
س



**ساختار لینوکس**
##
شما در لینوکس با مسیر ها و دایرکتوری ها به صورت درختی کار میکنید.
و تمام اطلاعات شما و اطلاعات سیستم در یک فایل و دایرکتوری مشخص هستند

برای درک بهتر این مهم که تمام اطلاعات لینوکس باید در پوشه ها و مسیر های مشخص باشند و شما هم باید 
از این پیروی کنید بیایید تا برای اولین قدم به پوشه ها و مسیر هایی که روی سیستم شما هست یک نگاهی بیندازیم
با رفتن به پوشه 
**home**
که روی صفحه دسکتاب خودتون داریدش و انتخاب گزینه 
**other locations**
از منوی سمت چپ
سپس به مسیر 
**Ubuntu**
به این پوشه
**root**
هم گفته میشود .

بروید که احتمال خیلی زیاد اولین گزینه هستش
یک پوشه براتون باز میشه با این فایل ها :

bin                home               mnt   sbin.usr-is-merged  usr
bin.usr-is-merged  lib                opt   snap                var
boot               lib64              proc  srv
cdrom              lib.usr-is-merged  root  swap.img
dev                lost+found         run   sys
etc                media              sbin  tmp


بیاییم با یک سری از این دوستان آشنا بشیم:))))

📁
**bin**
- دستوراتی که کامند های سیستمی حساب میشوند
- و مجوز های بالا تر برای اجرا میخوان
مثل :‌
    apt , apt-get, sudo
    به زودی با این دوستان هم آشنا میشیم .

📁
**lib**
- کتابخانه های ضرروی که اکثرا مربوط به فایل های 
    bin , sbin
- هستند

📁
**usr**
- محتویات این پوشه شبیه به همین مسیر 
    root
- هستش ولی یکی از تفاوت های این پوشه سطح دسترسی  هستش ! همه یوزر ها به اپ ها و پکیج های این پوشه دسترسی ندارند !

📁
**opt**
- این مسیر برای ذخیره برنامه های جانبی که از طریق های مختلف نصب میشوند هستش مثل :
    Vscode ,Chrome


📁
**boot**
- شامل فایل ها و پکیج هایی برای راه اندزی




## به هیچ عنوان دستشون نزنید :D☢️


📁
**etc**
- فایل های تنظیمات و پیکر بندی سیستم و برنامه ها اینجا قرار دارند
- Emarge to main Configurations
- مثلا تنیظمات برنامه ای مثل 
    Mysql , Vscode

📁
**dev**
- فایل های مربوط به سخت افزار متصل به سیستم مثل
    keyboard , mouse , headset

📁
**var**
- محل ذخیره لاگ ها و اتفاقات برنامه ها و یک جورایی محل ذخیره اطلاعات موقت
    log , cache ,
    tmp =>شاید با اون دو تا آشنایید ولی این یکی یکسری اطلاعات رو که باید برای یک مدت ذخیره باشند 
   رو ذخیره میکند

📁
**media**
- اطلاعات دستگاه های خارجی مثل
    USB
- اینجا ذخیره میشوند

📁
**mnt**
## 🩼

## About Folders and Files In Linux
هر فایل و فولدر در لینوکس دارای سه ویژگی هست :‌
۱. مالک 
    - owner
۲. گروه بندی مالک
    - Owner's Group(Primary Group)
۳. دسترسی 
    - Permissions
که شما میتونید این اطلاعات رو با اجرای دستور زیر در مسیر مورد نظر به دست آورید 


```bash
ls -l

```

## Commands
## خروجی دستورات
در لینوکس دستورات ما در ترمینال میتونن دو خروجی داشته باشند
<br>
1.STDOUT :
<br>
    - خروجی استاندارد و درست
    <br>

2.STDERR : 
<br>
    - اررور استاندارد
    

**تصب برنامه در لینوکس**

بر خلاف ویندوز که با 
installer , .exe
ها نصب برنامه ها انجام میشوند
در لینوکس ما باید با 
**پکیج ها**
کار کنیم و سر و کله میزنیم
این پکیج ها رو میشه به دو دسته تقسیم کرد
## ۱.   Software Package : 
    - فایل های اجرایی
    - dependecies : وابستگی ها
    - اسکریپت نصب و پیکربندی ها
## ۲. Package Managers : 

    - ابزاری که به کمک آن ها میشه پکیج ها رو 
    - نصب و به روز رسانی و مدیریت کرد ابزاری که در کامند لاین استفاده میشه ازشون مثل
    - apt , apt-get
    - ی جورایی میشه گفت مغز متفکر لینوکس هستش😶‍🌫️
    - در واقع با این ابزار میدونه چه فایل هایی کجا ذخیره میشه که و مسیر های پکیج های یک برنامه رو برای انجام عملیات های مختلف میدونه

خب پس نصب برنامه ها در لینوکس یعنی همون دانلود کردن و اضافه کردن یک پکیج در مسیر و دایرکتوری درست
شما میتونید پکیج و فایل مد نظرتون رو از مسیر های مختلفی دانلود کنید مثلا از سایت رسمی فایل اون برنامه رو دانلود کنید!
اما این مسیر توسیه نمیشه چون که ما قراره در کامند لاین  و با دستورات لینوکس کار کنیم !
پس لینوکس تکنولوژی های زیادی در اختیار ما قرار گذاشته !مثلا
apt , snap
این ها دو تا از مسیر های مختلف نصب برنامه ها هستند
البته با یکسری تفاوت اول بریم یک نگاه بهشون بندازیم
## snap ##  
برای درکش بهتره که به 
AppStore , PlayStore
تشبه ش کنم!
یعنی با این دستور شما دسترسی پیدا میکنید به یک سری از برنامه هایی که مورد تایید و قابل اعتماد هستند
یکی دیگه از ویژگی های اسنپ این هستش که حجم فایل هاش به علت اینکه تمامی برنامه ها در یک پکیج هستند و نصبش طولانی تر هست و زمان بوتش کند تر میشه و به خاطر این هستش که ایزوله هستش و اینجاست که شما ممکنه یک کتابخانه یا 
dependency
که برای چند برنامه مورد نیاز هستش رو با اسنپ چندین بار دانلودش کنید !
یعنی یک کار و یک دیتای تکراری رو دانلود میکنه !
تکرار کابوس تکنولوژي!؟
#
💭
نظر شما چیه ؟💭
💭
#

## نحوه نصب با استفاده از اسنپ ##
```bash

- sudo snap install spotify

```
sudo : super user do
    - سوپر یوزر عزیز میخواد انجام بده 😏
snap  : فراخوانی پکیج و دسترسی به دستورات اسنپ

isntall : نصب کن:/

spotify : اسم برنامه مورد نظر

خب **‌apt** 
 ی جورایی به همین صورت کار میکنه اما یکی از تفاوت هاش اینه که اگر اپی رو با اسنپ نصب کنید به صورت خودکار به روز رسانی میشه! 
## 
و یک تفاوت دیگر هم این هستش که ای پی تی دانلود هاش و مسیر های دانلودش رو از ریپو های مختلف و البته قابل اعتماد و تایید شده برمیداره پس شما این قابلیت رو با ای پی تی دارید که میتونید ورژن های مختلفی از یک برنامه رو نصب کنید !
یعنی
apt
میره و از لینک های مورد تایید گیت هاب برنامه هارو برمیداره! 
در ضمن در ادامه مسیر و تنظیمات ریپو و مسیر دانلود ای پی تی که گفتم رو بهتون میگم :D
و با دستور 

```bash
 sudo apt update
```

میتونیم دسترسی ها و لینک هارو به روز رسانی میکند
<details>
 و از سایت 
http://ir.archive.ubuntu.com/ubuntu
و یکسری دیتا رو میگیره که این دیتا ها مربوط هستند به لیست اخرین پکیج ها و ورژن های آنها و لیست خودش ور آپدیت میکنه

ℹ️
این دستور هیچ پکیجی و نرم افزاری رو اپدیت نمیکنه فقط آدرس دهی و دیتاهای آخرین ورژن پکیج ها و نرم افزار هاتون رو به روز میکند !
ℹ️
```bash 
sudo apt supgrade
```
و با این دستور روی لیست آپدیت های گرفته شده اقدام میشود !
</details>


# 


خب بریم سراغ کار با 
😃
## apt 😃 ##


# مسیر لیست ها و آدرس ها 
در مسیر روت قرار بگیرید و کد های زیر را اجرا کنید
``` bash
ls /etc/apt
``` 
ls : لیست فایل های مسیر مورد نظر رو میده !
etc/apt : آدرسی و یک روت 

``` bash 
cat /etc/apt/sources.list.d/ubuntu.sources

```
cat : با این محتویات مسیر و فایلی که ازش میخواییم رو نشون میده !
برای اینکه سرچ کنیم و ببینیم که آیا اپ مورد نظر ما جزو ریپو ها و مسیر های ای پی تی هست کامند زیر را اجرا میکنیم !!
```bash 
 - apt search appName
 ```
 apt : یک دستور و کامند لاین هستش برای مدیریت و دسترسی به برنامه های نصب شده روی سیستم

 search : جستجو کردن :D

 appName : اسم اپ مورد نظر

 نتیجه این کامند لیستی از ورژن های مختلف با نام های مختلف هستش ! که با برداشتن نام یکی از آن ها میتونیم با کامند زیر نصبش کنیم

## ** نصب یک برنامه با ای پی تی **

```bash
- apt install appNameBySearch
```
    - install = نصب کردن برنامه

**remove app **
برای پاک کردن یک برنامه با دستور زیر کار میکنیم
```bash
- apt remove appName
```
**نکته**
توجه داشته باشید اگر با دستور بالا یک برنامه رو پاک و حذف کنیم یکسری اطلاعات و پکیج هایی که همراه با این برنامه نصب شده اند هنوز سر جاشون هستند!
کجا؟🤔


## نصب یک برنامه خاص ##

خب همونجوری که گفتیم با استفاده از 
apt
یک سری آدرس مشخص رو میگرده و چک میکنه که آيا برنامه مورد نظر ما هست داخل اون ها یا نه!

حالا شما میخوایید یک برنامه رو بگیرید که جزو برنامه های و ریپو های 
apt
نیستن !!
</n>
بهش میگیم 
</n>

## Personal Private Archive ##

<h3>
 استفاده از ریپوزیتوری های اختصاصی 
</h3>
🚸
وقتی دارید از این روش استفاده میکنید خیلی حواستون باشه  و مطمین باشید از پکیجتون
🚸

```bash
sudo add-apt-repository ppa:deadsnakes/ppa 

```
با این دستور این ریپو به ریپو های دیفالت اپت اضافه میشه !
و سپس میتونیم  با دستور
```bash
sudo apt appName
```
پکیج مورد نظر و ادایی خودتون رو نصب کنید !





